import React from 'react';
import { Outlet } from 'react-router';
import SiteLayout from '../layout/SiteLayout';

const Main = () => {
    return (
        <SiteLayout>
            
        </SiteLayout>
    );
};

export default Main;